# Puerto del backend (desarrollo local)
PORT=8080
# Ruta local de SQLite (relativa a la raíz del repo)
SQLITE_PATH=data/norte.db
# TTL (días) para purgar tombstones y BLOBs huérfanos
TOMBSTONE_TTL_DAYS=90
# Límite máximo de upload de imágenes (bytes)
MAX_UPLOAD_BYTES=10485760
