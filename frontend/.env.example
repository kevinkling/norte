# URL base del backend (sin barra final), ej: https://norte-api.tudominio.com
# Dejar vacío en desarrollo local: Vite proxea /api a http://127.0.0.1:8080 (ver vite.config.ts)
# En build para producción con front y back en distinto host, es OBLIGATORIO.
VITE_API_BASE_URL=
